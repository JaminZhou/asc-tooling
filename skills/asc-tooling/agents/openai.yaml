interface:
  display_name: "ASC Tooling (Archived)"
  short_description: "Historical reference for the retired Ruby CLI."
  default_prompt: "Inspect historical $asc-tooling behavior only; use upstream asc skills for current App Store Connect work."

policy:
  allow_implicit_invocation: false
